# 🧠 Cortex v2.4 迭代计划

> **基于 v2.3 迭代后的剩余优化项**  
> **当前基线**: v2.3.0-rc.1 (29/53 项已修复) | **剩余**: 24 项  
> **评估日期**: 2026-05-06

---

## 目录

- [Sprint A: 架构基础设施](#sprint-a-架构基础设施)
- [Sprint B: 安全与功能补完](#sprint-b-安全与功能补完)
- [Sprint C: 开发者体验与运维](#sprint-c-开发者体验与运维)
- [综合路线图](#综合路线图)

---

## Sprint A: 架构基础设施

> 目标：为长期扩展打好基础  
> 难度：🔴 中-高 | 预计：3-5 天

### A1. Storage 接口拆分

| 项目 | 说明 |
|:-----|:------|
| **问题** | 当前 `Storage` 接口有 40+ 方法，是 God interface |
| **方案** | 拆分为 `DocumentReader`/`DocumentWriter`/`Searcher`/`CacheManager`/`AuthStorage` 等小接口 |
| **收益** | 接口隔离、便于测试 mock、新存储实现只需实现需要的接口 |
| **风险** | 改动面大，涉及所有存储调用方 |

### A2. 消除包级别全局变量

| 项目 | 说明 |
|:-----|:------|
| **问题** | `config.go` 的全局 `cfg`、`metrics.go` 的 `Registry` 等 |
| **方案** | 改为依赖注入方式，通过构造函数传参 |
| **收益** | 单元测试可隔离，并发安全天然保证 |

### A3. 请求上下文传播

| 项目 | 说明 |
|:-----|:------|
| **问题** | `indexer.go` 内部多处使用 `context.Background()` |
| **方案** | 从 API handler 向下传递 `context.Context`，实现超时/取消传播 |
| **收益** | 慢查询可取消、资源及时释放 |

### A4. 嵌入错误自动降级

| 项目 | 说明 |
|:-----|:------|
| **问题** | Ollama 挂掉时索引无感知，数组为空的 chunk 仍然保存 |
| **方案** | embedding 失败时自动切换 FTS5-only 模式，记录降级事件到 metrics |
| **收益** | 系统对下游依赖故障有弹性 |

---

## Sprint B: 安全与功能补完

> 目标：补全安全短板，增强用户可见功能  
> 难度：🟡 低-中 | 预计：3-5 天

### B1. 认证端点暴力破解防护

| 项目 | 说明 |
|:-----|:------|
| **问题** | `/auth/login` 和 `/auth/register` 无 IP 级限流 |
| **方案** | 添加 per-IP token bucket，5 次失败后锁定 15 分钟 |
| **风险** | 低 — 独立中间件，不改现有逻辑 |

### B2. 索引排除规则

| 项目 | 说明 |
|:-----|:------|
| **问题** | `node_modules/`、`.git/`、`.opencode/` 等目录被索引，产生大量噪声 |
| **方案** | 在配置中添加 `index.exclude` 列表，默认排除常见无关目录 |
| **收益** | RAG 上下文质量显著提升 |

### B3. 非活跃用户处理

| 项目 | 说明 |
|:-----|:------|
| **问题** | 已停用账号登录返回 "user not found"，误导 |
| **方案** | 返回 "account deactivated" 错误码 |
| **收益** | UX 明确 |

### B4. CLI JSON 输出模式

| 项目 | 说明 |
|:-----|:------|
| **问题** | 当前 CLI 只输出纯文本，不方便被其他工具调用 |
| **方案** | 添加 `--json` 标志，输出结构化 JSON |
| **收益** | 可被 jq/脚本/CI 管道消费 |

### B5. 搜索结果分页

| 项目 | 说明 |
|:-----|:------|
| **问题** | 搜索不支持分页，一次性返回所有结果 |
| **方案** | 添加 `offset` 参数，支持分页遍历 |
| **收益** | 大规模知识库更好用 |

### B6. MCP 错误码标准化

| 项目 | 说明 |
|:-----|:------|
| **问题** | 工具返回的纯文本错误不符合 MCP 规范 |
| **方案** | 使用 `mcp.InvalidParamsError` / `mcp.InternalError` 等标准错误码 |
| **收益** | 兼容 MCP 客户端错误处理 |

### B7. 记忆批量删除

| 项目 | 说明 |
|:-----|:------|
| **问题** | 只支持单条记忆删除 |
| **方案** | 添加 `cortex_memory_delete_batch` 工具或支持 ID 列表 |
| **收益** | 记忆管理更灵活 |

---

## Sprint C: 开发者体验与运维

> 目标：提升运维能力和开发者效率  
> 难度：🟢 低 | 预计：2-3 天

### C1. Web 管理界面 (Mini)

| 项目 | 说明 |
|:-----|:------|
| **内容** | 单页 HTML 管理界面，嵌入二进制 |
| **功能** | 索引状态、搜索测试、记忆管理、系统健康 |
| **技术** | Go embed + 原生 HTML/JS，零外部依赖 |
| **收益** | 不用 curl 也能操作 |

### C2. Swagger/OpenAPI 文档

| 项目 | 说明 |
|:-----|:------|
| **方案** | 添加 swagger 注释，生成 `docs/openapi.json` |
| **收益** | 对接方无需读源码 |

### C3. Grafana Dashboard

| 项目 | 说明 |
|:-----|:------|
| **方案** | 提供 Grafana JSON 模板，配合 Prometheus 指标 |
| **收益** | 系统状态一目了然 |

### C4. Docker 多阶段构建

| 项目 | 说明 |
|:-----|:------|
| **方案** | `Dockerfile` 改为多阶段构建，镜像从 ~500MB 降到 ~30MB |
| **收益** | 部署更快、更安全 |

### C5. Release Workflow 自动化

| 项目 | 说明 |
|:-----|:------|
| **方案** | CI 自动生成 changelog + 编译所有平台二进制 + 发布 GitHub Release |
| **收益** | 发版一键完成 |

---

## 综合路线图

```
Sprint A (3-5天)   架构基础设施     🔴 Storage接口拆分 / 全局变量消除 / 上下文传播
                                    └── 嵌入降级策略

Sprint B (3-5天)   安全与功能补完   🟡 暴力破解防护 / 索引排除 / CLI JSON
                                    └── MCP错误码 / 记忆批量删除 / 分页

Sprint C (2-3天)   开发者体验与运维 🟢 Web管理界面 / Swagger / Grafana
                                    └── Docker多阶段 / Release自动化

总工期: 8-13 天 | 目标版本: v2.4.0
```

---

## 优先级建议

```
🔥 最推荐先做 (快速见效)        🟢 Sprint B2 索引排除规则
                                     🟢 Sprint B4 CLI JSON 输出

💪 中期投入 (影响最大)          🔴 Sprint A1 Storage 接口拆分
                                     🟡 Sprint B1 暴力破解防护
                                     🟢 Sprint C1 Web 管理界面

🏗️ 长期布局 (打好基础)          🔴 Sprint A2 全局变量消除
                                     🔴 Sprint A3 上下文传播
                                     🟢 Sprint C5 Release 自动化
```

---

*本计划基于 v2.3.0-rc.1 评估生成，可根据实际需求调整优先级。*
